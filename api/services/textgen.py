from typing import List


def predict_next_words(prompt: str, max_prediction_len=3, max_num_of_predictions=3) -> List[str]:
    # predictions: List[str] = model.predict_next_words(prompt)
    predictions: List[str] = []
    return predictions


def generate_text(prompt: str, max_gen_len=10, max_num_of_generations=1) -> List[str]:
    # predictions: List[str] = model.generate_text(prompt)
    generate_texts: List[str] = []
    return generate_texts
